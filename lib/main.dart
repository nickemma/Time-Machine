import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: const Text('LazyCoders Developers'),
          centerTitle: true,
        ),
        body: const Center(
          child: Text('Hello Lazy Flutter Developers!'),
        ),
      ),
    ));
